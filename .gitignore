node_modules
# Keep environment variables out of version control
.env
dist
/generated/prisma
# Carpeta generada en cada build (el frontend compilado, copiado adentro
# del backend para que sobreviva al runtime en Render) — no es del frontend,
# ese sigue versionado normalmente
inventario-back/public/